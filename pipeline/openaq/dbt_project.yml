name: 'openaq'
version: '1.0.0'
config-version: 2

profile: 'openaq'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["data-tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  openaq:
    staging:
      +materialized: view
    marts:
      +materialized: table
