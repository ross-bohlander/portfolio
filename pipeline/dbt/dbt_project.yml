name: 'yield_curve'
version: '1.0.0'
config-version: 2

profile: 'yield_curve'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  yield_curve:
    staging:
      +materialized: view
    marts:
      +materialized: table
